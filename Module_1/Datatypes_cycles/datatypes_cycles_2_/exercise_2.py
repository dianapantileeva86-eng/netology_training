"""
Задание 2
Дана переменная, в которой хранится список поисковых запросов пользователя (пример структуры данных приведен ниже, но запросы потенциально могут быть любые). Вам необходимо написать программу, которая выведет на экран распределение количества слов в запросах в требуемом виде.

Пример работы программы:

queries = [
‘смотреть сериалы онлайн’,
‘новости спорта’,
‘афиша кино’,
‘курс доллара’,
‘сериалы этим летом’,
‘курс по питону’,
‘сериалы про спорт’,
]
Результат:

Поисковых запросов, содержащих 2 слов(а): 42.86%
Поисковых запросов, содержащих 3 слов(а): 57.14%
"""
queries = [
    'смотреть сериалы онлайн',
    'новости спорта',
    'афиша кино',
    'курс доллара',
    'сериалы этим летом',
    'курс по питону',
    'сериалы про спорт',
]
from collections import defaultdict
word_count = defaultdict(int)
total_queries = len(queries)

for query in queries:
    words = len(query.split())
    word_count[words] += 1

for words in sorted(word_count):
    percentage = (word_count[words] / total_queries) * 100
    # Правильное склонение слова "слово"
    if words % 10 == 1 and words % 100 != 11:
        word_form = "слово"
    elif 2 <= words % 10 <= 4 and (words % 100 < 10 or words % 100 >= 20):
        word_form = "слова"
    else:
        word_form = "слов"
    print(f"Поисковых запросов, содержащих {words} слов(а): {percentage:.2f}%")